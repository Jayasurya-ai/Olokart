<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#323232</item>
        <item name="colorAccent">#42a626</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="DialogAnimation_2">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>

    <style name="CateAnimation">
        <item name="android:windowEnterAnimation">@anim/lefttoright</item>
        <item name="android:windowExitAnimation">@anim/rightotleft</item>
    </style>

    <style name="SubCateAnimation">
        <item name="android:windowEnterAnimation">@anim/rightotleft</item>
        <item name="android:windowExitAnimation">@anim/lefttoright</item>
    </style>

</resources>